<div class="content px-4">

    <h5>
        <strong>@SelectedChallenge.Order. @SelectedChallenge.Title</strong>
    </h5>

    <div class="challenge-point"><h5><span class="badge badge-info">Points: @SelectedChallenge.Point</span></h5></div>

    <hr />

    <div>
        <Markdown Text="@SelectedChallenge.Body" />
    </div>

    @foreach (var question in SelectedChallenge.Questions)
    {
        <QuestionSubmit QuestionChallengeResource="@question"></QuestionSubmit>

        //<EditForm Model="@ProblemSubmit" OnValidSubmit="@HandleValidSubmit" class="form-inline">
        
            @*
            <div>
                <Markdown Text="@problem.Description" />
            </div>
            <div>
                <div class="form-group row">
                    <label for="inputPassword" class="col-sm-2 col-form-label"></label>
                    <div class="col-sm-10">

                    </div>
                    <button class="btn btn-info edit-btn" @onclick="() => SubmitAnswer(problem.Id)"> Submit </button>
                </div>
            </div>
            </EditForm>*@
        }
</div>